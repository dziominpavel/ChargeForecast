package com.example.chargeforecast

import org.junit.Assert.assertTrue
import org.junit.Test

/**
 * Минимальный smoke-test: проверяет, что тестовый контекст собирается.
 * Реальные тесты добавляются вместе с функционалом.
 */
class PlaceholderTest {
    @Test
    fun smokeTest_passes() {
        assertTrue(true)
    }
}
