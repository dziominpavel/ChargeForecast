## 1. Регрессия (сначала красные)

- [x] 1.1 Добавить unit-тест в `SpeedFusionTest` с числами живой сессии (S1 — null, S2 — 0.003, S3 — 2.0, возраст 120 сек, уровень 87) и проверить, что он падает: итог 0.003 вместо 2.0
- [x] 1.2 Добавить engine replay-тест (уровень 87→90 за 90 сек, счётчик frozen, ток 7 мА, без приора) и проверить, что он падает: скорость ≈0.003 и остаток в сотни часов вместо ≈2 %/мин и <15 мин

## 2. Вето движущегося уровня

- [x] 2.1 Реализовать в `SpeedFusion.fuse` пост-фильтр по design D1–D4 (пороги 0.2 %/мин и 0.3, `refining=false` при срабатывании, покрытие tier1/tier2/tier3) и проверить: тесты 1.1 и 1.2 зеленеют, все существующие тесты `SpeedFusionTest` зелёные без правок
- [ ] 2.2 Прогнать `.\gradlew.bat test` целиком до зелёного и проверить отсутствие регрессий в остальных сюитах

## 3. Живая проверка

- [ ] 3.1 Собрать debug APK, повторить живую сессию на устройстве автора и проверить: при движущемся уровне скорость больше не показывает 0, остаток — минуты, а не сотни часов
