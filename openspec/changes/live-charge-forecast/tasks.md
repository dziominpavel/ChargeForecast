## 0. Чистка

- [x] 0.1 Удалить `datastore-preferences` из `gradle/libs.versions.toml` и `app/build.gradle.kts` и проверить: `.\gradlew.bat assembleDebug` зелёный, поиск `datastore` по проекту пустой

## 1. Battery-слой

- [x] 1.1 Создать модель BatterySnapshot и репозиторий на ACTION_BATTERY_CHANGED + BatteryManager и проверить unit-тестом маппинг intent в snapshot
- [x] 1.2 Добавить nullable чтение тока/напряжения с fallback на дельту уровня и проверить unit-тестами оба пути (датчики есть / датчики 0 или отсутствуют)
- [x] 1.3 Подключить репозиторий к StateFlow с обновлением при изменении батареи и проверить на устройстве: уровень/статус/тип подключения обновляются без ручного опроса

## 2. Движок прогноза

- [x] 2.1 Реализовать расчёт скорости %/мин по скользящему окну 10 мин с весом последних минут и отбросом выбросов и проверить unit-тестами на синтетических сессиях (ровная, шумная, первые минуты)
- [x] 2.2 Реализовать экстраполяцию до 100% с коэффициентом доводки после 80% и статусами точности (собираю данные / примерно / точно) и проверить unit-тестами сценарии из specs/charge-forecast
- [ ] 2.3 Откалибровать окно и коэффициент доводки spike-замером на реальном устройстве автора и зафиксировать значения константами с комментарием

## 3. Сервис и шторка

- [x] 3.1 Создать ForegroundService с каналом уведомлений и ongoing-уведомлением (уровень, скорость, остаток, точность) и проверить: уведомление появляется на зарядке, тап открывает приложение
- [x] 3.2 Добавить BroadcastReceiver на POWER_CONNECTED/DISCONNECTED для автостарта/автостопа сервиса и проверить на устройстве: воткнул — сервис стартовал за 10 сек, выткнул — уведомление ушло за 30 сек
- [x] 3.3 Ограничить частоту обновления уведомления (не чаще 30 сек, не реже 60 сек, без звука/вибрации) и проверить длительной сессией 30 мин без спама

## 4. Экран и разрешения

- [x] 4.1 Создать ViewModel на StateFlow и заменить заглушку живым экраном сессии через токены Volt (без inline dp/цветов) и проверить: цифры экрана совпадают с уведомлением в пределах одного цикла обновления
- [x] 4.2 Запросить POST_NOTIFICATIONS в рантайме на Android 13+ с fallback-экраном при отказе и проверить на устройстве: отказ не роняет приложение, есть кнопка повтора
- [x] 4.3 Прописать service и разрешения в AndroidManifest.xml и показать ошибки системы видимо (Snackbar/текст, без silent-catch) и проверить: отказ старта сервиса виден пользователю

## 5. Сквозная проверка

- [x] 5.1 Прогнать `.\gradlew.bat assembleDebug` и `.\gradlew.bat test` до зелёного и проверить отсутствие lint-ошибок ContentDescription
- [ ] 5.2 Провести живую сессию зарядки 20+ мин на устройстве автора и проверить: до двух изменений уровня — «собираю данные» (но ток/вольтаж видны сразу), затем «примерно», после 10 мин «точно», на 80%+ прогноз не занижается, при вытыкании UI переходит в «не на зарядке»

## 6. Точность с первых секунд и шторка

- [x] 6.1 Внедрить счётчик-спидометр, трекер ёмкости, sysfs best-effort и ранний PRECISE по схождению и проверить unit-тестами (ровный счётчик, рефайн, санитайзер, схождение/расхождение)
- [x] 6.2 Запрашивать POST_NOTIFICATIONS при первом запуске и доподнимать сервис при выдаче и проверить: `assembleDebug` + `test` зелёные, диалог появляется на чистой установке, шторка живёт на зарядке

## 7. Приор из кэша и пульс шторки

- [x] 7.1 Использовать скорость прошлой сессии как приор с секунды 0 и прятать мусор рампы порогом 0.05 %/мин, проверить unit-тестами (приор, порог, доводка)
- [x] 7.2 Писать пульс шторки из сервиса и предупреждать на экране при простое >90 сек, проверить: `assembleDebug` + `test` зелёные

## 8. Фикс вечного «собираю данные»

- [x] 8.1 Мусор рампы не должен затенять приор: selectInstantSpeed с порогом и максимумом из опор, проверить unit-тестами (регрессия Huawei P60 Pro) и зелёной сборкой
