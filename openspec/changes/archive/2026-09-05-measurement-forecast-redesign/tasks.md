# Задачи change measurement-forecast-redesign

> **Решение автора (05.09.2026):** функционал шторки удалён из продукта. Задача 3.1 (адаптивный троттлинг шторки) — отменена; 3.2 остаётся в экранной части («Измеряю… N с» на экране), шторочная часть удалена. Задачи 3.3/3.4 — на устройстве автора.

## 0. Фаза 0 — быстрые победы (конвейер прежний)

- [x] 0.1 Д1: записывать `saveLastSpeed` только в конце сессии (переход active→inactive), убрать непрерывную запись из тика; `lastSpeed()` читать только из кэша; завести `SessionSummary` (endedAtMs, durationMs, gainedPct, avgCcSpeedPctPerMin по сэмплам после рампы и до 80%/CV, peakCurrentMa, chargeType) с сохранением в `SessionCache` и проверить unit-тестами: сводка пишется один раз, «прошлая» не равна текущей, отсечение на доводке не занижает avgCcSpeed
- [x] 0.2 Д2a: заменить `updateSessionMax` (пик тока) на EMA тока с tau=15 сек в instant-оценке и проверить unit-тестами: EMA гасит рампу и выбросы, регрессия на синтетике «рампа 0.1→1.5 А»
- [x] 0.3 Д2c: заменить ступеньку `base /= TAPER_FACTOR` на границе 80% линейной интерполяцией множителя 1.0→0.5 между 80% и 90% (временно, до фазы 2) и проверить unit-тестом: остаток на границе 79→80% меняется непрерывно, без скачка ×2
- [x] 0.4 Добавить регрессионный тест «25→85 мин» на синтетике (постоянная скорость + рампа тока) и прогнать `.\gradlew.bat test` до зелёного

## 1. Фаза 1 — секундный конвейер

- [x] 1.1 Перевести все расчёты времени на `SystemClock.elapsedRealtime` (дефолт инжектируемого clock в `BatteryMonitor` и `ChargeSessionMonitor`) и проверить unit-тестом: подстройка системных часов посреди сессии не меняет скорость/возраст
- [x] 1.2 Перевести POLL на 1 сек на зарядке (10 сек на разряде) и тикер на 1 сек на зарядке (30 сек в простое), убедиться что `sameReading` не глушит живой ток, и проверить unit-тестами интервалов по состоянию
- [x] 1.3 Реализовать S1 в `CounterSpeed`: МНК по окнам счётчика 30 сек + 2 мин (вместо одного 2-мин) и проверить unit-тестами: ровный счётчик, шумный счётчик (шум /5 против двух концевых точек), короткая сессия
- [x] 1.4 Создать чистый `SpeedFusion`: S1(30с)/S1(2м)/S2/S3 с fusion по ярусам возраста (0-20с → S2+приор; 20-90с → S1(30с) доминирует, S2 кросс-чек; >90с → S1(2м), S3 кросс-чек), при расхождении S1/S2 >40% — консервативная скорость и «уточняю», и проверить unit-тестами все ветки таблицы
- [x] 1.5 Перевести расчёт остатка в мкА·ч: `capacityUah - chargeCounterUah` делить на скорость (уровень — fallback), подключить fusion к `ChargeForecaster` и проверить unit-тестами: остаток убывает при стоящем уровне 1%, fallback-ветка без счётчика
- [x] 1.6 Создать чистый `DisplayFilter`: EMA tau=10 сек + округление до секунд каждый тик (без корзин) + защита от всплесков вверх (рост >25% только если держится 3 тика) и проверить property-тестом: при постоянной истинной скорости показанный остаток не возрастает после 90-й секунды
- [x] 1.7 Жизненный цикл: `plugDetectedAtMs` от POWER_CONNECTED/перехода isPlugged, `chargingConfirmedAtMs` от первого isCharging, замеры с plugDetectedAtMs, рампу не выбрасывать, и проверить unit-тестом: рампа Huawei P60 Pro не сдвигает секунду ноль
- [x] 1.8 Обновить контракты тестов `ChargeForecastTest`/`InstantSpeedTest`/`CounterSpeedTest` под новый конвейер (ни один тест не удалить без эквивалентной замены), добавить replay-сессии (ровная, рампа Huawei P60 Pro, шумный счётчик, без счётчика, без тока) и прогнать `.\gradlew.bat test` до зелёного (упоминавшийся в исходном плане адаптивный троттлинг шторки отменён вместе с удалением шторки; файл `NotificationThrottleTest` удалён)

## 2. Фаза 2 — CV-кривая и профили

- [x] 2.1 Заменить TAPER-константы кривой корзин 80-100 по 5% (дефолт 0.55/0.40/0.30/0.20), остаток = сумма по корзинам energy(bin)/(baseRate·factor(bin)) и проверить unit-тестом: переход через 80% плавный, интеграл сходится с линейной оценкой на 80%
- [x] 2.2 Реализовать обучение кривой по сессиям одного типа подключения с персистом в `SessionCache` (SharedPreferences, без новых зависимостей) и проверить unit-тестами: обучение по одной сессии, изоляция профилей AC/USB/wireless, восстановление из кэша

## 3. Фаза 3 — юзабельность и калибровка

- [x] 3.1 Сделать троттлинг шторки адаптивным 5-15 сек (или обновление по изменению текста) и проверить unit-тестом троттлинга: интервал сжимается при активной смене цифр и растёт при стабильных — **отменено вместе с удалением шторки; код удалён**
- [x] 3.2 Показывать «Измеряю… N с» с живым счётчиком на экране (тик 1 сек), текст в `strings.xml`, UI через токены Volt; шторочная часть отменена — и проверить: `.\gradlew.bat assembleDebug` + `test` зелёные, счётчик виден с первой секунды
- [ ] 3.3 Откалибровать константы spike-замером на устройстве автора (окна МНК, tau EMA, корзины доводки, порог консервативного веса) и зафиксировать значения константами с комментарием — закрывает задачу 2.3 change `live-charge-forecast`
- [ ] 3.4 Провести контрольную живую сессию 20+ мин и приёмку по чек-листу: остаток тикает каждую секунду (ч/мин/с) с первой валидной опоры; приор прошлой сессии не портится нулями; показанный остаток почти монотонно убывает (пересмотры ≤25%, редко); переход 80% плавный; ошибка «точно» ≤±10 мин; разряд возвращает опрос на 10 сек — закрывает задачу 5.2 change `live-charge-forecast`
